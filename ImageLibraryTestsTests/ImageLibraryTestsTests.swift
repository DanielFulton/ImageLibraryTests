//
//  ImageLibraryTestsTests.swift
//  ImageLibraryTestsTests
//
//  Created by Daniel Fulton on 8/5/16.
//  Copyright © 2016 GLS Japan. All rights reserved.
//

import XCTest
import Kingfisher
@testable import ImageLibraryTests

class ImageLibraryTestsTests: XCTestCase {
    
    override func setUp() {
        super.setUp()
        // Put setup code here. This method is called before the invocation of each test method in the class.
    }
    
    override func tearDown() {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
        super.tearDown()
    }
    
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct results.
    }
    
    func testPerformanceExample() {
        // This is an example of a performance test case.
//        let imageView = UIImageView(frame: CGRectMake(0, 0, 200, 200))
//        guard let url = NSURL(string: "http://apod.nasa.gov/apod/image/1605/IC2944_Campbell_960.jpg") else {
//            return
//        }
//        let asynchExpectation = self.expectationWithDescription("image download")
//        self.measureBlock {
//            // Put the code you want to measure the time of here.
//            imageView.kf_setImageWithURL(url, placeholderImage: nil, optionsInfo: nil, progressBlock: nil, completionHandler: { (image, error, cacheType, imageURL) in
//                XCTAssertNotNil(image, "No Image returned \(error)")
//                asynchExpectation.fulfill()
//            })
//            [self .waitForExpectationsWithTimeout(10, handler: { (error) in
//                
//            })]
//        }
    }
    
}
